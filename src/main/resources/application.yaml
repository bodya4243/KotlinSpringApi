spring:
  application:
    name: kotlin-spring-api
  datasource:
    url: jdbc:postgresql://localhost:5438/kotlin-spring-db
    driver-class-name: org.postgresql.Driver
    username: root
    password: 1234
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

message: Hello from default profile

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mariadb://localhost:3308/kotlin_spring_db
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234
  jpa:
    database: MYSQL
    database-platform: org.hibernate.dialect.MariaDBDialect

message: Hello from dev profile